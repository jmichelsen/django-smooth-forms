============
Installation
============

At the command line::

    $ easy_install django_smooth_forms

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_smooth_forms
    $ pip install django_smooth_forms
